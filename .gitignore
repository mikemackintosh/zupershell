# Swift Package Manager
.build/
.swiftpm/

# Xcode (if anyone opens the SPM package in Xcode later)
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/

# The built .app is a reproducible artifact — build it with ./bundle.sh
Zupershell.app/
# Legacy pre-rename artifact
myterm.app/

# macOS crumbs
.DS_Store
